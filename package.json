{
  "name": "atkit",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --parallel --filter=!./example run build",
    "build:watch": "pnpm -r --parallel run build:watch",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "cli": "pnpm --filter @atkit/cli start"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
